# Arch/Hyprland overrides, loaded after the shared config.

# Hyprland already blurs windows; do not double-blur in ghostty.
background-opacity = 0.95
background-blur = false

# The shared config sets 24 for the mac/ubuntu machines; reset to default here.
font-size =

keybind = ctrl+delete=text:\e[3;5~
